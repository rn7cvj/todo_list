import 'package:flutter/material.dart';

class EditTaskPortait extends StatelessWidget {
  const EditTaskPortait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
